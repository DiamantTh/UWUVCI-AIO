namespace UWUVCI.Core.Models;

/// <summary>
/// ROM / base region.
/// Values match the legacy GameBaseClassLibrary.Regions enum exactly.
/// </summary>
public enum Region
{
    EU = 0,
    US = 1,
    JP = 2,
}
